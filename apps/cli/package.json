{
  "name": "@skillport/cli",
  "version": "1.3.0",
  "description": "SkillPort CLI — open-source secure skill distribution for OpenClaw & Claude Code. Export, scan, sign, convert, and install AI skill packages.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/skillport-dev/skillport.git",
    "directory": "apps/cli"
  },
  "homepage": "https://skillport.market",
  "bugs": "https://github.com/skillport-dev/skillport/issues",
  "keywords": [
    "skillport",
    "openclaw",
    "ai-skills",
    "cli",
    "mcp",
    "skill-marketplace"
  ],
  "bin": {
    "skillport": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "tag": "latest"
  },
  "scripts": {
    "build": "rm -rf dist && node scripts/bundle.js",
    "build:dev": "tsc",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist",
    "prepack": "rm -rf dist && node scripts/bundle.js"
  },
  "dependencies": {
    "chalk": "^5.4.0",
    "commander": "^13.0.0",
    "inquirer": "^12.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@skillport/core": "workspace:*",
    "@skillport/scanner": "workspace:*",
    "@skillport/shared": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
